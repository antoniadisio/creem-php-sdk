# Harness Support

Shared helper code for the committed `playground/` harness belongs here.

Keep resource-specific action files out of this directory.
Webhook receiver and capture-inspection helpers also belong here when they support contributor live-workflow needs without changing the outbound `run.php` action model.
